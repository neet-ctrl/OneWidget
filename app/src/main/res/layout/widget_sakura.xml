<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:contentDescription="@string/widget_name">

    <ImageView
        android:id="@+id/widget_art"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:scaleType="fitXY"
        android:src="@drawable/widget_art_clean" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginTop="31dp"
        android:gravity="bottom"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:includeFontPadding="false"
            android:letterSpacing="0.01"
            android:shadowColor="#55FFFFFF"
            android:shadowDx="0"
            android:shadowDy="1"
            android:shadowRadius="2"
            android:text="07:40"
            android:textColor="@color/clock_pink"
            android:textSize="42sp"
            android:textStyle="normal" />

        <TextView
            android:id="@+id/widget_meridiem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="5dp"
            android:layout_marginStart="5dp"
            android:fontFamily="sans-serif-medium"
            android:includeFontPadding="false"
            android:text="PM"
            android:textColor="@color/clock_pink"
            android:textSize="13sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_date"
        android:layout_width="190dp"
        android:layout_height="27dp"
        android:layout_gravity="center_horizontal|top"
        android:layout_marginTop="87dp"
        android:background="@drawable/date_pill"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:paddingBottom="2dp"
        android:text="Sunday, 18 May"
        android:textColor="@color/date_pink"
        android:textSize="11sp" />

    <LinearLayout
        android:layout_width="92dp"
        android:layout_height="wrap_content"
        android:layout_gravity="end|center_vertical"
        android:layout_marginEnd="23dp"
        android:gravity="center_vertical"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_temperature"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:includeFontPadding="false"
            android:text="34°"
            android:textColor="@color/weather_dark"
            android:textSize="24sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_condition"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:includeFontPadding="false"
            android:maxLines="1"
            android:translationY="-1dp"
            android:text="Cloudy"
            android:textColor="@color/weather_dark"
            android:textSize="11sp" />
    </LinearLayout>
</FrameLayout>