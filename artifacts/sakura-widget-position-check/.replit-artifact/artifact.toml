kind = "design"
previewPath = "/__widget-mockup/"
title = "Sakura Widget Position Check"
description = "A focused visual mockup that places the live time, date, and weather text into the supplied empty Sakura widget template for approval before updating the native Android source."
version = "1.0.0"
id = "artifacts/sakura-widget-position-check"
router = "path"

[[services]]
name = "Component Preview Server"
paths = [ "/__widget-mockup/" ]
localPort = 18374

[services.development]
run = "npm run dev"

[services.env]
PORT = "18374"
BASE_PATH = "/__widget-mockup/"
